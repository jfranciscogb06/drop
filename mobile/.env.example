# API Configuration
API_BASE_URL=http://localhost:3000/api
SOCKET_URL=http://localhost:3000

# Stripe Publishable Key
# Get this from https://dashboard.stripe.com/apikeys (use the publishable key, NOT the secret key)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Note: For iOS/Android, you may need to use your local network IP instead of localhost
# Example: API_BASE_URL=http://192.168.1.100:3000/api
# Find your IP: ifconfig (Mac/Linux) or ipconfig (Windows)
